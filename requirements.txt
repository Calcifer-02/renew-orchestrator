# Backend — FastAPI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# MCP server
mcp>=1.0.0

# Bridge — orchestration kernel (stdlib only, no extra deps)

# Optional: workflow YAML loading (uncomment if using refactor_workflow.yaml)
# pyyaml>=6.0

# Dev / testing
# pytest>=8.0
# httpx>=0.27  # for FastAPI TestClient
